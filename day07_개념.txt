day07_개념

복습 -> 제어문(반복문, 기타제어문), 이중 반복문
0. 복습
1) 연산자
	최 단 산 쉬 관 리 삼 대

- 단항연산자
	+, -, ++, --, !

- 산술연산자
	+ - * / %
	정수 + 정수 = 정수
	정수 + 실수 = 실수
	실수 + 실수 = 실수

- 쉬프트연산자(비트)
	좌쉬프트 A << B
	우쉬프트 A >> B
	Logical 쉬프트 A >>> B  음수도 양수처럼 0으로 채워줌(부호 무시하고 채움)
	<<< X (왼쪽으로 하려하면 오류가 발생,사용X)

- 관계연산자
	<, >, <=, >=, ==, !=
	결과는 항상 boolean(true, false)타입으로 나온다!

- 논리연산자
	&, |, !
	true & true

	& | ^ ~
	1010 & 1011	=> 1010
	1010 | 1011	=> 1011
	1010 ^ 1011	=> 0001
	~1010 ->  ~10   => -11
	

- 삼항연산자
	조건식 ? 참일때의 값 : 거짓일떄의 값

- 대입연산자


2) 조건문

- if문, switch문

	if~else if~else문
	if (조건식) {
		조건식1이 참일 때 실행할 문장;
	}else if (조건식2){
		조건식1이 거짓이고 조건식2가 참일 때 실행할 문장;
	}else{
		조건식1과 조건식2가 모두 거짓일 때 실행할 문장;
	}

- switch문

	switch (값) {
	case 값1:
		값1과 같을 때 실행할 문장;
		break;

	case 값2:
		값2와 같을 때 실행할 문장;
		break;
	
	case 값3:
		값3과 같을 때 실행할 문장;
		break;
	default:	
		위의 case의 값과 모두 같지 않을 때 실행할 문장;
		break;
	}

1. 반복문
   특정 조건을 만족할 때까지 코드 블록을 반복 실행

1) for문
	반복 횟수가 정해져 있을 때 주로 사용
	
	for(초기식; 조건식; 증감식) {
		조건식이 true일 동안 실행
	}
	
	초기식 : 처음에 설정할 값(변수)	=> int i(index 줄임말) = 0;
	조건식 : 조건식의 결과가 true면 중괄호 영역안의 문장을 실행
		false면 중괄호 영역을 들어가지 않고 for문 탈출
	증감식 : 값을 얼마나 증감할지 정하는 식 => i++
	
2) while문
	조건식이 참인 동안 반복한다
	실행 중에 조건식이 거짓이 되어야지만 무한루프(무한반복)에 빠지지 않는다

	while (조건식) {
		조건식이 참일 동안 실행할 문장;
 	}

3) do~while문
	while문과 동일하지만 조건을 보지않고 무조건 최소 한번은 실행해야할때 사용한다
	while문과 다르게 조건식 뒤에 {}가 아니라 ;가 붙음	

	do {
		실행할 문장;
	}while(조건식);


2. 기타제어문
	반복문에서 주로 사용한다
	단, break문은 switch문에서도 사용한다

1) break문
	즉시 해당 중괄호 영역을 탈출한다
	break문 아래 코드를 스킵한다(실행하지 않는다)

2) countinue문
	즉시 다음 반복으로 넘어간다
	continue문 아래 코드를 스킵한다(실행시키지 않는다)




파일 압축해제 
File - Open Projets from File System - Directory - 파일 찾아서 넣기 - Finish


















































